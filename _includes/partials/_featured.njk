<div class="featured-container">
  {# {% set displayPosts = site.posts | where: 'show', true %}

  {% for post in displayPosts limit:1 offset:0 %}
  {% set category = metadata.categories[post.category] %}
  {% set author = metadata.authors[post.author_id] %}
    <div class="featured-column">
      <div class="featured-post" style="background-image: url('{{ post.featured_image}}');">
        <a href="{{ metadata.baseurl }}{{ post.url }}">
          <!-- <h1 class="post-title">{{ post.title }}</h1> -->
        </a>
      </div>
      <article class="post-item">
        <a href="" class="blog-category"># {{ category.display_name}}</a>
        <a href="{{ metadata.baseurl }}{{ post.url }}">
          <div class="post-info">
            <div class="post-time-info">
              <time class="post-meta" datetime="{{ post.date }}">
                {{ post.date | date: "%b %-d, %Y" }}
              </time>
            </div>
            <h1 class="post-title">
              {{ post.title }}
            </h1>
            <p class="paragraph">
              {{ post.content | truncatewords:40 | strip_html}}
            </p>
          </div>
        </a>
        <span class="author">By <a href="{{author.profile_url}}" class="author" target="_blank" >{{ author.display_name }}</a></span>
      </article>
    </div>
  {% endfor %} #}
  asdad
  <div class="bottom-row">
    {# {% for post in displayPosts limit:1 offset:1 %}
    {% set category = metadata.categories[post.category] %}
    {% set author = metadata.authors[post.author_id] %}
      <div class="featured-column">
        <div class="featured-post" style="background-image: url('{{ post.featured_image}}');">
          <a href="{{ metadata.baseurl }}{{ post.url }}">
          </a>
        </div>
        <article class="post-item">
          <a href="" class="blog-category"># {{ category.display_name}}</a>
          <a href="{{ metadata.baseurl }}{{ post.url }}">
            <div class="post-info">
              <div class="post-time-info">
                <time class="post-meta" datetime="{{ post.date }}">
                  {{ post.date | date: "%b %-d, %Y" }}
                </time>
              </div>
              <h1 class="post-title">
                {{ post.title }}
              </h1>
              <p class="paragraph">
                {{ post.content | truncatewords:20 | strip_html}}
              </p>
            </div>
          </a>
          <span class="author">By <a href="{{author.profile_url}}" class="author" target="_blank" >{{ author.display_name }}</a></span>
        </article>
      </div>
    {% endfor %}
    {% for post in displayPosts limit:1 offset:2 %}
    {% set category = metadata.categories[post.category] %}
    {% set author = metadata.authors[post.author_id] %}
      <div class="featured-column">
        <div class="featured-post" style="background-image: url('{{ post.featured_image}}');">
          <a href="{{ metadata.baseurl }}{{ post.url }}">
          </a>
        </div>
        <article class="post-item">
          <a href="" class="blog-category"># {{ category.display_name}}</a>
          <a href="{{ metadata.baseurl }}{{ post.url }}">
            <div class="post-info">
              <div class="post-time-info">
                <time class="post-meta" datetime="{{ post.date }}">
                  {{ post.date | date: "%b %-d, %Y" }}
                </time>
              </div>
              <h1 class="post-title">
                {{ post.title }}
              </h1>
              <p class="paragraph">
                {{ post.content | truncatewords:20 | strip_html}}
              </p>
            </div>
          </a>
          <span class="author">By <a href="{{author.profile_url}}" class="author" target="_blank" >{{ author.display_name }}</a></span>
        </article>
      </div>
    {% endfor %} #}
    asdas
  </div>
</div>
