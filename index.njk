---
layout: layouts/default.njk
---

{% include 'partials/_featured.njk' %}
{# TOOD: Fix featured and delete these recent posts #}
<h1>Recent posts</h1>
{% set postslist = collections.posts | head(-3) %}
{% set postslistCounter = collections.posts | length %}
{% include "partials/_postslist.njk" %}

<div class="page-content">
  <div class="wrapper">
    <div class="home">
      <h1 class="page-heading">
      </h1>
      <section>
        
        {# TODO: Delete header #}
        <h1>Old posts</h1> 
        {% set postslist = collections.posts | head(3) %}
        {% set postslistCounter = collections.posts | length %}
        {% include "partials/_postslist.njk" %}
      </section>
    </div>
    <iframe src="https://xmartlabs.substack.com/embed" width="100%" height="320" style="border:1px solid #EEE; background:white;" frameborder="0" scrolling="no"></iframe>
  </div>
</div>
